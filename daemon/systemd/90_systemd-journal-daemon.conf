# This is a service configuration drop-in for journald.conf installed by
# systemd-journal-viewer.
# It overrides the defaults so the application can read a log file.

[Journal]
Storage=persistent
RateLimitIntervalSec=0
SystemMaxUse=20M
MaxRetentionSec=0

